#include <iostream>
#include <vector>

vector <int> moveZeros(vector <int> a, int n) {
    int temp[];
    for(i=0; i<n; i++) {
        if(a[i] !=0) {
            temp.add(a[i]);
        }
    }

    for(i=0; i<temp.size(); i++) {
        a[i] = temp[i];
        noofnZ= temp.size
    }
    for(i=noofnZ; i<n; i++) {
        arr[i] = 0;
    }

    return 0;
}